var reqConfig = function(){
    var getConfig = function(type = null){
        delete require.cache[require.resolve('./MyConfig.js')];
        config = require('./MyConfig');
        if(config[type])
            return config[type];
        return config;
    }

    return{
        load: getConfig
    }
}

module.exports = reqConfig;